{% extends sonata_block.templates.block_base %}

{% block block %}
    <table class="table table-condensed">
    {% for key,value in results %}
        <tr>
            <th>{{ key }}</th>
            <td>{{ value }}</td>
        </tr>
    {% else %}
            No feeds available.
    {% endfor %}
    </table>
{% endblock %}
