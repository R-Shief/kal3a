{% extends 'base.html.twig' %}

{% block body %}
    <h1>StreamParameters</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ streamParameter.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('streamparameters_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('streamparameters_edit', { 'id': streamParameter.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
